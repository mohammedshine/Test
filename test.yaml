id: dashboard

info:
  name: Backpack Admin Panel
  author: shine
  severity: info
  description: description

requests:
- method: GET
path:
      - '{{BaseURL}}/admin/login'

matchers-condition: and
matchers:
      - type: word
        words:
          - '<title>Login :: Backpack Admin Panel</title>'
      - type: status
        status:
          - 200
